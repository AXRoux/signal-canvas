# Client (Vite) — no secrets in repo
VITE_PLATFORM=web
VITE_API_BASE=
VITE_MVP_MODE=true

# Cloudflare Worker secrets (set via: wrangler secret put NAME)
# JWT_SECRET=
# ENCRYPTION_KEY=
# TELEGRAM_BRIDGE_URL=
