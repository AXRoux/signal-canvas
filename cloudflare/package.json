{
  "name": "signal-canvas-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply signal-canvas --local",
    "db:migrate:remote": "wrangler d1 migrations apply signal-canvas --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250204.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.65.0"
  }
}
